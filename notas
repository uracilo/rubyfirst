js 30mb por proceso
5 dias se hizo 


lenguaje compilado (java o c)

son rapidos

maquina virtual lee solo el lenguaje
jvm java virtual machine

lenguaje interpretado

js programacion orientada a eventos js


si haces un archivo que tiene la extencion rb es un documento de rubi

entonces se corre como ruby  script.rb

POO
actores que hacen acciones con atributos
POAcciones
funciones que se desencadenan en un momento pertinente

un objeto es la instancia de una clase

un objeto es un campo semantico que corresponde a una clase
atributos (adjetivos) y funciones (verbos)

.chomp quita un salto de linea que es "\n"


symbol dar logica a mi código


esta es la funcion modulo %


